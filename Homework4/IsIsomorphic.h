#pragma once

#include <vector>
#include <iostream>

class IsIsomorphic
{
    public:
        IsIsomorphic();
        bool areTheyIsomorphic(std::vector<std::vector<size_t> >,std::vector<std::vector<size_t> >);
    private:
};


